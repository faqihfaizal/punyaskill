University Dashboard (trimmed)
This project was constructed from the uploaded Zest React template and includes only the 'university' related components and views.
What I changed:
- Added .nvmrc with Node version '22'
- Created a minimal Vite + React project (package.json, vite.config.js)
- Copied University-related components and views into src/
Notes:
- You still need to run `npm install` or `pnpm install` to fetch dependencies.
- Some components may reference CSS, images or older libraries; you may need to adjust imports or replace deprecated packages.
